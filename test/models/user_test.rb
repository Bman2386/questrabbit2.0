require 'test_helper'

class UserTest < ActiveSupport::TestCase
    # this test doesn't work
    # test "first user is Guest" do
    #     first_user = User.first
    #     guest = User.find_by(username: "Guest")
    #     assert_equal(first_user == guest, 'Guest is not 1st user')
    # end
end

